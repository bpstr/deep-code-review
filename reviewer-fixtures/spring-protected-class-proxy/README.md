Spring Framework 6.2, Java 21. Component scanning and a transaction manager are configured. The application uses @EnableTransactionManagement(proxyTargetClass = true) with the default transaction attribute source, no publicMethodsOnly override, and no AspectJ. Both classes below are in the same package. Caller receives the Spring-created ServiceTarget proxy by constructor injection.
