Laravel 12 application excerpts. Invoice is a normal Eloquent model. SendInvoice implements ShouldQueue, uses the default Redis connection and has no beforeCommit override. Its consumer loads the invoice on another database connection. The active queue configuration below is loaded as config/queue.php. Authorization, validated input and side-effect idempotency are handled outside these excerpts.
